# 多功能工具脚本配置文件
# 作者: AI Assistant
# 版本: 1.0

# 默认超时时间（秒）
DEFAULT_TIMEOUT=30

# 默认重试次数
DEFAULT_RETRY_COUNT=3

# 默认输出目录
DEFAULT_OUTPUT_DIR="./output"

# 日志级别 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL="INFO"

# 是否启用详细输出
VERBOSE_MODE="false"

# 网络相关配置
NETWORK_TIMEOUT=10
NETWORK_RETRY=3

# 备份相关配置
BACKUP_COMPRESSION_LEVEL=6
BACKUP_EXCLUDE_PATTERNS="*.tmp,*.log,*.cache"

# 监控相关配置
MONITOR_INTERVAL=1
MONITOR_DURATION=60

# 系统相关配置
SYSTEM_CHECK_INTERVAL=5
SYSTEM_ALERT_THRESHOLD=80 